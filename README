This is C extension for PHP.
It's contains multibyte string compatible Damerau-Levenshtein.
Function name is damerau_levenshtein(string str1, string str2, [int cost_ins, int cost_sub, int cost_del, int cost_tran]).

INSTALATION AT MAC OS X


Download actual php source code ad ./php directory

In console:

phpize
./configure --with-damerau
make
make install


Add extension damerau.so to your php.ini file:

extension=damerau.so

INSTALATION AT LINUX, GENTOO


In console:

phpize
aclocal
libtoolize --force
autoheader
autoconf
./configure --enable-damerau
make
make install


Add extension damerau.so to your php.ini file:

extension=damerau.so
